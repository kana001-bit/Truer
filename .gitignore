node_modules/
dist/
.tmp/
*.log

# examples を実行した生成物（propose/apply/preview の出力）は追跡しない
examples/*.proposal.json
examples/*.preview.svg
examples/*.fixed.dxf
examples/out/

# 個人のエディタ装飾（タイトルバー色など）は各自ローカルに置き、repo では追跡しない
.vscode/settings.json

# ツール固有のポインタは各自ローカル。クロスツールの正本メタは .claude/skills/*/agents/*.yaml が担う
.codex/

# 作業メモ・タスク仕様・ブランチ記録は非公開。公開するのは docs/ 直下の設計/リファレンスだけ。
# うっかり `git add .` で private ノートを追跡しないよう明示的に無視する。
docs/work/
docs/task-specs/
docs/branch/
